<h1 class="page-title">Welcome to <%= @user.username %>'s page.</h1>
<div class="row">
	<div class="col-sm-4 col-sm-offset-4">
		<%= gravatar_for @user, size: 160 %>
	</div>
</div>
<h4><%= @user.username %>'s Articles.</h4>
<%= render 'articles/articles', articles: @user.articles %>
